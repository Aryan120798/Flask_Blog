Corey Schafer - Flask

1.
    routing
    run code in debug mode

2. 
    Templates
    For, ifelse in JINJA2 {{%  %}}
    bootstrap
    css file in flask(url_for())

3.
    wtforms- form extension in flask(pip install it)
    flash message
    redirect user to difn page after registeration is done
    validation feedback
    {{ ... }} to access variables in the Templates
    {% ... %} is code blockS

4. 
    sqlAlchemy
    sql constraint check(not_null, primary key, unique)

5. 
    Bcrypt - password hashing
    

